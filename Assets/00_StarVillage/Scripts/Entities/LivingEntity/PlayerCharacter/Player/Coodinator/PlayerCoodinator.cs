using UnityEngine;

/// <summary>
/// 플레이어 객체의 행동 조율
/// </summary>
public class PlayerCoodinator : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
